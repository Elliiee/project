# project

We need to initialize the project directory as a node project root directory: npm init

Next, install our four dependencies:
npm install mysql2 express nodemon forever express-handlebars

Open db-connector.js in your text editor and update your database name and password.
